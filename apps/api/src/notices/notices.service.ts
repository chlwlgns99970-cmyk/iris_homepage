import { Injectable, NotFoundException } from '@nestjs/common'; import { PrismaService } from '../infrastructure/prisma.service';
@Injectable() export class NoticesService { constructor(private prisma:PrismaService){} async list(page:number,limit:number){ const where={status:'PUBLISHED' as const,publishedAt:{lte:new Date()}}; const [items,total]=await this.prisma.$transaction([this.prisma.notice.findMany({where,orderBy:{publishedAt:'desc'},skip:(page-1)*limit,take:limit,select:{id:true,type:true,title:true,summary:true,publishedAt:true}}),this.prisma.notice.count({where})]); return {items,page,limit,total}; } async one(id:string){ const item=await this.prisma.notice.findFirst({where:{id,status:'PUBLISHED',publishedAt:{lte:new Date()}},select:{id:true,type:true,title:true,summary:true,content:true,publishedAt:true}}); if(!item) throw new NotFoundException({code:'NOTICE_NOT_FOUND',message:'공지사항을 찾을 수 없습니다.'}); return item; } }
